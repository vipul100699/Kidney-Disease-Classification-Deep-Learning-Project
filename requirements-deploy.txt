# Deployment Requirements (Lighter for Cloud)
tensorflow-cpu==2.20.0
Flask==3.0.3
Flask-Cors==5.0.0
numpy==2.1.3
Pillow==10.4.0
python-box==7.2.0
pyYAML==6.0.2
ensure==1.0.4
python-dotenv==1.0.0

# Remove heavy packages for deployment
# No need for: notebook, matplotlib, seaborn, dvc, dagshub, mlflow
